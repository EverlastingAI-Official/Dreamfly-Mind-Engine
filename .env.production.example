# Compose interpolation only; application settings live in server/.env.production.
# Always use --env-file .env.production -f compose.production.yaml.
COMPOSE_PROJECT_NAME=dreamfly-production
IMAGE_TAG=local
WEB_PORT=18080
